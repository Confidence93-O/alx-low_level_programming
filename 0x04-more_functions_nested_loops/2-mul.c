#include "main.h"

/**
 * mul _Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply between x parm and y param
 */

int mul(int x, int y)
{
	return(x * y);
}
