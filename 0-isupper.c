#include "main.h"

/**
 * _isupper - Check is a letter is upper
 * @x: The number to be checked
 * Retur: 1 for upper or 0 for any else
 */
int _isupper(int x)

{
	if (x >= 65 && <= 98)
	{
	return (1);
	}
	return (0);
}
